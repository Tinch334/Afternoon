# Paragraph typesetting
Implements a basic typesetting algorithm for breaking paragraphs into lines. Handles glue stretching and compression, box splitting not implemented.

To run open `index.html` on any modern browser.